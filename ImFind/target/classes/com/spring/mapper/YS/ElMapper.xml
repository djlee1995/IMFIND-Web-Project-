<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace="com.spring.mapper.MemberMapper는 MemberMapper인퍼페이스(MemberMapper.class)의 이름을 사용한다. -->
<mapper namespace="com.spring.mapper.YS.ElMapper">


	<!-- id는 Mapper.java(여기선 MemberMapper.java)에 동일하게 사용해야된다. -->
	
	<!-- (202012311235) -->
	<!-- SELECT * FROM emp_copy where deptno = #{deptno} order by empno  -->
	
	<select id="getElsedata" resultType="ElVO">		
		
		SELECT * FROM LostPost order by Lost_PostNum 		
			
	</select>  
	
	
	
	<!-- 테이블 수정하고 나서, 아래와 비슷 한 느낌 (하지만 다른) 쿼리로 수정해야 한다.
	 ID: apple로 로그인 했으면 여러가지 데이터 중에 ID : apple이 가진 값들이 모두 나와야 하기 때문이다.   -->
	 
	<!-- 테스트 초기 이기 떄문에 DB컬럼 의 조건과 컬럼 이름이 변동이 계속 있으므로 테스트 수행시 확인 하기 바란다. 
		ex. Pat테이블은 PatPost 테이블이 되었다. 
		ex2.PatPost테이블의 회원아이디는 ID이고, Member 테이블의 회원아이디는 id , 다시 LostPost테이블의 회원ID는   Id 
		therefore, 매번 확인 하기 바란다. (202012311249)--> 
	 
	 
	 
	 
	 
	<!--일단 특정 ID의 값이 나오는 것 먼저? --> 
	<!--  
	<select id="getmypostdata" resultType="ElseVO">		
		
		SELECT * FROM LostPost where Id = #{Id} 		
			
	</select> -->
	<!-- 위에것이 출력이 되면 아래도 주석 풀고 진행 한다.  -->
	<!-- 결제 내역  -->
	<!--  
	<select id="getmypostdata2" resultType="ElseVO">		
		
		SELECT * FROM Pay where Id = #{Id} 		
			
	</select>
	-->
	
	<!-- 위에것이 출력이 되면 아래도 주석 풀고 진행 한다.  -->
	<!-- 반려동물게시판 글 목록 내역  -->
	<!--  
	<select id="getmypostdata3" resultType="ElseVO">		
		
		SELECT * FROM PatPost where Id = #{Id} 		
			
	</select>
	-->
	
	
	<!-- 위의 쿼리로 입력한 값이 나오는지 일단 연결 성공을 먼저 볼것. 이후 
	A001 인 아이디가 로그인 했을경우 -> 로그인 정보 일치 여부 상단에 (jsp에)배치 해주고
	+ A001이 등록한 글과 결제한 내역만을 (다른 아이디 데이터 오면 안됨) 끌고 온다.)
	 -->
	 
	 
	 
	
	
</mapper>